# The Public facing version of my app
It has the same files, but removes any sensitive information like API keys

This app helps sailors get results from websites. It mainly supports clubspot but will eventually support other sites as well. It aggregates data in a supabase backend, gets live information using cron jobs, and sends notifications to users. The goal is to also help sailors create profiles and resumés for themselves.

